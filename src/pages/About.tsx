import AboutPageComponent from "../components/AboutPageComponent";

export default function About() {
  // This is a simple about page component that can be used in the in any part of the website.
  return (
    <div>
      <AboutPageComponent />
    </div>
  );
}